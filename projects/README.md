# projects/ — standalone mini-projects

Self-contained source experiments: one project per folder, each with its own short README (what it is, how to run it). Source + text only — no binaries, no large assets (see `../AGENTS.md`).
