# notes/ — gists, snippets, durable notes

Small reusable code, curl recipes, and memory overflow from the local decision log when it's worth keeping durably. One topic per file, dated in the filename or header. Text only; no secrets (see `../AGENTS.md`).
