# SAM 3D → body poses → Blender presets

- **Type:** explore / build
- **What:** Use SAM 3D to derive body poses from reference material, then map them onto the Blender preset-action/camera-move library (see [preset-action-movies-v2v](preset-action-movies-v2v.md)).
- **Why I want to try it:** Turns "I want this movement" (a video/image of a person) into concrete pose sequences our Blender scenes can use — closes the gap between reference inspiration and generated action.
- **Fits stage:** content→scene (feeds the previs stage).
- **Open questions:** SAM 3D output format → how to import as poses (rig? keyframes?); accuracy on stylized/white-model bodies; pairing with [pose-camera-library-node](pose-camera-library-node.md).
- **Status log:**
  - 2026-09-06 — added to backlog (want-to-try).
